{
  "name": "mirditalab",
  "version": "1.0.0",
  "description": "Lab website",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:molstar": "npm ci --prefix minimolstar && npm run build --prefix minimolstar"
  },
  "devDependencies": {
    "puppeteer": "^24.34.0"
  }
}
